- name: ippudu min and max values chudam
  hosts: mylovelyhost
  vars:
   sudheergadu:
    dog: "animal"
    sindhu: "lovelyfigure"
    ramya: "No figure"
  tasks:
  - name: before convert to the items
    ansible.builtin.debug:
      msg: "the dictionary is {{sudheergadu}} "

  - name: After converting into items
    ansible.builtin.debug:
      msg: "coverting into dictionany {{ sudheergadu| dict2items}}"
