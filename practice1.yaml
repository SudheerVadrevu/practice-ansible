- name: we will install the nginx and run it
  hosts: mylovelyhost
  become: yes
  tasks:
  - name: install the nginx
    ansible.builtin.dnf:
     name: nginx
     state: latest
  - name: run the nginx
    ansible.builtin.service:
     name: nginx
     state: started
     enabled: yes